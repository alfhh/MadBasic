program ciclos;

methods:
    int rFactorial(int n){
	vars:
		int acum;
		
    		if(n - 1 <= 0){
    			acum =  1;
    		} else {
    			acum = rFactorial(n - 1) * n;
    		}
    		
    		return acum;
    }

main:
vars:
    int a, b, c,d, x;

b = 5;
a = rFactorial(b);
print(a);
end
